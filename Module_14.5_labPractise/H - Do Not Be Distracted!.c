#include<stdio.h>
#include<string.h>
int main()
{
    int n_case;
    scanf("%d",&n_case);
    for(int i =1; i<=n_case; i++)
    {
        int str_size;
        scanf("%d",&str_size);
        char str[str_size+1];
        scanf("%s",str);
    }

    return 0;
}
